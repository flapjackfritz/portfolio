version: 0.1

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run webpack

artifacts:
  files:
    - index.html
    - blog.html
    - portfolio.html
    - favicon.ico
    - images/*
    - dist/bundle.js
    - dist/styles.css
